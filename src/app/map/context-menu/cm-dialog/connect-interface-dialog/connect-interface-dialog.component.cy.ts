import { ConnectInterfaceDialogComponent } from './connect-interface-dialog.component'

describe('ConnectInterfaceDialogComponent', () => {
  it('should mount', () => {
    cy.mount(ConnectInterfaceDialogComponent)
  })
})